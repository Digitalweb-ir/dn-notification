# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Distribution
build/
dist/
*.egg-info/

# Telethon session
*.session
*.session-journal

# Env / secrets
.env
.env.local

# Logs
*.log
logs/

# Editor
.vscode/
.idea/
.DS_Store

# Claude code
.claude/

# Node
node_modules/
# package-lock.json is committed on purpose: the release workflow runs
# `npm ci` to install semantic-release, and `npm ci` requires a
# committed lockfile (lockfileVersion >= 1). Do NOT add it back here.